import type { RoleplayService } from '../roleplay-service.js';
import type { EvolutionAdapter } from './model.js';
/** Existing witnessed-source/controller/GM validation owns every fiction change. No real-world feedback becomes an event. */
export declare function fictionEvolutionAdapter(service: RoleplayService): EvolutionAdapter;
//# sourceMappingURL=fiction-adapter.d.ts.map