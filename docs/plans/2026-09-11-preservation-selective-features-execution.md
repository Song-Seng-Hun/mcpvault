# Preservation, selective features and measured context cost

Status: application release deployed, live-verified, committed and pushed to the user's existing fork main on 2026-09-12. NAS direct-write hardening remains incomplete with the user's explicit approval. Performance claims are limited to the recorded synthetic measurements.

## Contract

- Immutable imported originals, disposable separate parsed/indexed derivatives, editable evidence-linked Wiki knowledge. Never rewrite/move/delete existing originals or introduce a second history system.
- Confidential originals and all derivatives require verified local inference and current document authority; no cloud fallback or egress. Department membership, host execution, document rights, feature selection and human-owner optional-work consent are independent checks.
- Explicit replaceable, versioned feature selections; new installs Wiki only, conveniences independently selectable. This PC explicitly selects current features without authorizing external providers, payments or another human's agents.
- Keep five MCP control-plane tools, dynamic endpoints, bounded responses, revision checks, Markdown/Git authority. Preserve existing data and unrelated changes.
- Improve repeated document reads, cache persistence, bounded ranking, situation candidate discovery, memory admission, endpoint fingerprints, selective initialization/invalidation and bookkeeping routing. Claims require before/after evidence and no quality regression.

## Execution checklist

- [x] P1: application original mutation boundary and lossless new source capture, separate derivatives, regression tests. NAS direct-write protection is deferred, not established.
- [x] P2: host-verified confidential source/derivative authorization and private cache protection.
- [x] P3: feature selection manifest, dependency validation, dispatch/discovery/init/pulse gating, host configuration.
- [x] P4: verified department defaults and company/interdepartment visibility; owner consent and local bookkeeping integration. No provider or execution grant added.
- [x] P5: document I/O/cache/ranking/memory, situation metadata and endpoint/cache invalidation optimizations.
- [x] P6: deterministic synthetic scale/quality and fair vanilla evaluation harness; no paid runs or measured monetary-cost claim.
- [x] P7: independent spec and quality reviews, targeted tests, build, full tests, diff/staged/secret review.
- [x] P8: NAS-backed application deployment and live smoke, rollback artifacts, source+dist commit and verified push to existing user-fork main. NAS direct-write hardening remains deferred.

## Ownership and execution constraints

Main worker owns integration, originals, access, feature configuration and deployment. One isolated sidecar at a time; staffing, feature configuration, enterprise department registry, endpoint digest, streaming revalidation and owner-consent pure policy have scoped implementation/review evidence. These are not integrated completion claims. Test/build slots are serialized (maxWorkers=1); retain at least 3 GiB available RAM. No branch/worktree, package publication, release, upstream PR, forced Git operation, data migration/reclassification or external model activation.

## Verification record

Final checkpoint (2026-09-12): application deployment, live verification, source+dist commit and user-fork push passed. NAS direct-write hardening is explicitly deferred.

- Implementation commit `8b91121210745bcab472ddf2bd0affa1cb261e8a` was pushed
  without force to `Song-Seng-Hun/mcpvault` main. A fresh remote-tip read matched
  the local commit. This completion-record follow-up changes no tested or
  deployed application bytes. The unrelated seven research files remain excluded.

- Final gate `final-validated-v3`: all 32 test processes exited zero. The collector
  verified all 439 configured files with no overlaps or unreceipted reports:
  6,145 assertions passed, zero failed, three declared Windows skips. The source,
  test and compiled-output basis is
  `731fbdd2cfe1ceee5e571e023b59e3ab92f4a3973aa796403c5b5d50ba022b48`.
  Minimum sampled free RAM across this complete run was 5.071 GiB. No preceding
  diagnostic reports were combined into this gate.
  The three skips cover Windows-invalid pipe filenames, POSIX-only permission
  inspection, and file symlink creation refused with EPERM/EACCES.
- The fresh single-threaded build passed. The separate explicit real-process
  benchmark smoke passed all 13 Node tests. All 396 staged implementation paths
  passed the path gate, pattern-only credential screen and whitespace check;
  seven unrelated research documents remain untracked and excluded.
- Exact-process preflight passed for the 864 matching release files, private
  eleven-feature configuration and canonical data. The first activation attempt
  stopped before service shutdown because Windows PowerShell stripped quotes in
  an inline Node memory query. The old writer and launcher hash were reverified;
  a direct Windows memory query replaced only that local deployment check.
  Successful activation recovered the confirmed dead writer locks through the
  audited recovery APIs and started the preservation release as PID 26484.
- Live public MCP acceptance passed: fixed five tools, one orientation and exact
  primary public read, active-only discovery, exact-link/revision note reading,
  selected document reading, retrieval guidance, advisory configuration granting
  no authority, and refusal of optional world reads without owner consent. The
  verification registered no account, posted nothing and executed no model.
- Streamed checks before/after cutover and again after live acceptance matched
  all five original files (120,134 bytes), their private backup and restored copy.
  The two Roleplay turns and single economy journal entry retained their exact
  bytes and checkpoints (sequences 2 and 1); only writer ownership changed.
  Prior release and launcher rollback artifacts remain available. No NAS source
  permissions were changed; direct SMB/admin writes are not prevented by this
  application release. The user confirmed Telnet disabled after the failed
  synthetic protection probe.

### Earlier checkpoints (historical, not current completion claims)

- All 32 `final-0933` shards completed, covering all 439 configured files and
  6,148 assertions: 6,143 passed, two failed and three source-declared Windows
  skips. This is diagnostic evidence, not a passing gate. The only failures were
  the sorted-result identity expectation described below and a story catalog
  fixture that omitted new owner admission. Test-only corrections now verify
  internal cache reuse and current returned rows, and explicitly exercise both
  owner-admitted story schemas and denial without consent while retaining all
  prior capability/read-only/schema constraints. All 51 tests across those two
  files plus shared-index storage and endpoint registry passed (minimum sampled
  6.363 GiB). Production code is unchanged. A fresh full fixed-basis run remains
  required after these two fixture corrections; earlier pass counts are not mixed.
  Independent applied-diff review found no P1/P2. The next single-threaded build
  passed with minimum sampled 6.527 GiB. The explicit
  `PRESERVATION_BENCHMARK_SMOKE=1` Node run passed all 13 tests, including the
  separate real-process 100-document quality/freshness probe (minimum sampled
  7.032 GiB). `final-validated-v3` now reruns all 32 shards on the new fixed basis;
  activation is bound to that collector, not to either preceding diagnostic run.

- The `final-0824` continuation stopped on one real assertion failure at 198/439
  files: the semantic foreground-busy test had no private derivative storage, so
  the new fail-closed storage guard returned before inference admission. Only its
  fixture now provisions a real private temporary storage root and closes it;
  all original busy/retry assertions remain. The complete file passed 10/10 and
  a fresh single-threaded build passed (minimum sampled 6.250 GiB free).
  `final-0933` starts an entirely new fixed-basis 32-shard run; no older report is
  combined into it. Workers remain serial, with 512 MiB worker and 384 MiB
  coordinator heaps, explicit exit receipts and the same RAM stop guard. The
  earlier counts below are diagnostics, not acceptance of this new basis.
- `final-0933` found one obsolete array-identity assertion in
  `vault-index-cache-estimation.test.ts`: sorted cache hits now reapply the current
  PathFilter and return a fresh filtered array while retaining internal cache rows.
  The planned test-only correction keeps cyclic YAML rejection, revision and row
  count checks, and asserts returned content, retained entry identity and internal
  cache-array identity separately. Independent contract review agreed; the
  existing warm-filter regression already covers revocation without invalidation.
  Remaining shards continue diagnostically before any test-file edit/new basis.
  A separately confirmed existing runtime symlink-permission skip is accepted only
  for its exact test on Windows with status `skipped`, never unexecuted `pending`.
  Eight verifier regressions pass; the new skip distinction first failed. This
  changes no product guard or test skip condition.

- Final-gate correction: a 192 MiB worker exhausted its heap during the legitimate
  8 MiB document-structure input case. Vitest exited 1 but emitted `success: true`
  with 13 passed and nine pending assertions (its aggregate pending count was zero).
  Earlier assertion-only counts below are historical diagnostics, not final gates.
  The verifier now requires actual exit zero, a matching source basis and report
  hash, complete assertion coverage and only source-declared conditional skips.
  Six verifier regressions pass; five failed against the former success-only rule.
  Unreceipted older reports are retained but excluded and their files rerun. Only
  two shard receipts were reconstructed from explicitly recorded exit-zero results.
  A fresh 512 MiB document-structure run passed all 22 tests with minimum sampled
  6.062 GiB free. Other files use 256 MiB workers in fresh serialized processes;
  the supervisor retains its 3.8/3.5 GiB start/stop thresholds. At 09:13 the
  stricter gate covered 83/439 files, 1,366 passed and one declared Windows skip;
  remaining files continue. No product assertions or code were weakened.
  An independent verifier review found that an empty assertion list could still
  count a file as covered. A new regression first failed; both empty passed and
  empty pending suites are now rejected even with an always-allow skip callback.
  All seven verifier tests pass and independent closure review found no remaining
  P1/P2. The staged 394 implementation paths passed the path gate, pattern-only
  credential screening and whitespace check; the seven unrelated research files
  remain untracked. Commit, activation and push still require complete regression.

- The queued metadata barrier, skill-root canonicalization and ordinary long-path
  fixes passed their targeted suites. A new request-wide barrier captures catalog
  generation only after queued metadata is prepared; later authority/generation
  fences remain in place. Broader Wiki regression passed 203 tests.
- Native semantic reuse formerly took 37.7 seconds for one case because every ACL
  check started PowerShell. The replacement reuses only a lazy native inspector,
  never an ACL result. The same case took 1.47 seconds; eight real Windows tests
  passed for current-ACL denial, restart, in-flight loss, malformed output,
  bounded transport and idle shutdown. Independent source security/quality review
  reported no P1/P2; Unicode/owner-change/timeout/host-exit coverage was not claimed.
- Semantic reuse, pulse and replica authority suites passed 78 tests. The obsolete
  manifest-failure fixture now targets the real private namespace, retaining the
  native database write and failure assertions. Replica fixture hub handles are
  now explicitly closed before teardown rather than left for garbage collection.
- Replica reads filter current document/owner/PathFilter access before rows,
  totals and paging. Request-bound sync omits global cursor/completion and marks
  absence unverified. New remote-write/replay failures were reproduced before
  actual destination, source and final-write guards. Pending bridge intents retain
  provenance; replay rechecks current consent for local and remote destinations.
  The latest complete bridge/protocol group passed 37 tests. Independent review
  findings for local reply provenance, outbox payload identity, exact-grant
  tombstones, retry order and interrupted actor creation were reproduced and
  corrected. Final scoped source rereview found no new P1/P2. The real two-server
  protocol uses a scenario-specific 15-second ceiling, not a suite-wide increase.
- Fresh build passed with minimum sampled 4.177 GiB free. Fixed-basis regression
  completed 15 of 32 shards, with 11 failing cases. The RAM guard stopped shard
  16 at 3.495 GiB; completed reports remain available but coverage is incomplete.
  Ten failures came from four older test setups: a generic source write, and
  optional-activity routing without explicit owner admission. Those fixtures are
  corrected, with two no-consent negative cases added. An initial five-file
  diagnostic was stopped by the RAM guard; after RAM recovered, all 43 tests
  passed with minimum 3.952 GiB free. The corpus evaluation passed in 4.95 seconds,
  too close to its former five-second ceiling; only this serial 120-query case
  now has a 15-second limit. Its full packet Recall@5/MRR were 0.935/0.862 versus
  lexical 0.921/0.840. The smaller 1,800-character packet scored 0.736/0.806:
  reduced context is not a quality-preserving cost-win claim. The old run
  fingerprint predates these test edits and is not the final test basis.
- The five repaired test files were followed by a successful fresh single-threaded
  build (minimum sampled 4.012 GiB free). The replacement fixed-basis run has
  completed shards 16–18: all 602 tests passed; the remaining 29 shards are still
  required. Earlier diagnostic shards are not merged into this new verification.
  Shard 19 was stopped at 3.495 GiB by the guard, with successful termination of
  its owned process tree and no final report. Later starts were deferred at
  3.729 and 3.543 GiB; neither started a test worker. Available RAM also sampled
  3.32–3.48 GiB between checks. Heavy verification paused at that point. No user-owned
  application was closed. The read-only evidence collector confirms 42 of 439
  configured test files have current-basis reports, without overlaps; the other
  397 remained unverified then. After the user closed other applications, free RAM
  recovered to about 6 GiB. A guarded 19–32 continuation started at 5.308 GiB;
  no source/test/build basis was changed and no second heavy job runs alongside it.
  Shards 19–20 subsequently passed, bringing coverage to 70 files / 987 tests;
  shard 21 stopped at 3.443 GiB and has no accepted report. Remaining coverage is
  now run one file per fresh process, with a 192 MiB JS heap. The collector accepts
  only disjoint reports newer than the unchanged basis and requires all 439 files
  from the actual Vitest include set, not a nominal shard count. At this checkpoint
  147 files / 1,902 tests have passed, with no accepted failure report. No code,
  test or compiled-file edits were made to obtain this resource-strategy change.
- Heavy runs are serialized; sampled minima vary and are recorded per run.
  The current supervisor starts at 3.8 GiB and stops its owned process
  tree at 3.5 GiB with 50 ms sampling; it is not an OS-wide hard guarantee.
- The user temporarily enabled NAS Telnet for a fixed read-only permissions probe,
  then confirmed stopping and saving it. Actual Samba storage is under
  `/mnt/VOL1/Data/llm_wiki/llm_wiki-nas-20260909`; checked source directories are
  writable. ACL/attribute utilities were not found in the six inspected executable
  locations; this does not establish lack of kernel support. No source bytes or
  NAS permissions changed. The user approved a bounded helper/protection operation.
  A local ARM probe now builds and its exact-byte transfer accepts the original
  executable and rejects a one-byte corruption. Its only modes inspect the five
  originals' metadata and test immutable flags on a newly created synthetic file.
  Safety review found and closed two issues: copying now has a byte ceiling,
  and the shell completion command is sent only after the binary verification ACK.
  The 35,268-byte helper was uploaded outside the Vault originals into a new
  temporary NAS staging folder; the staged SHA-256 matches the local executable.
  The user enabled LAN-only Telnet and entered the password in a local secure
  dialog. The first detached popup exited before any probe; a retained execution
  session then successfully verified the exact NAS-private binary before running.
  All five original metadata reads succeeded: mode 0777, flags 524288, ACL-query
  errno 95. On the NEW synthetic fixture, flags 524304 blocked rename and delete
  but did not block pwrite through the already-open writable descriptor. The
  synthetic byte check correctly failed; its original flags were restored to
  524288 and the changed fixture was retained. The helper therefore did not claim
  successful protection. No original or share permission changed. Existing open
  writable handles must not be ignored when considering an original-protection
  operation; no protection application is authorized by this failed self-test.
  The user confirmed disabling and saving Telnet afterward. No password or
  authentication transcript was saved. RAM has recovered enough for the guarded
  serial regression, and no further application closure is requested.
- A fresh read-only byte check again matched all five originals, the private
  backup and restored copies (120,134 bytes). It also recorded the two canonical
  Roleplay turns and one economy journal entry, their full streamed byte hashes
  and unchanged checkpoints. Both writer locks identify the existing PID 22344.
  These preflight observations do not replace a cutover-time recheck or a
  post-deployment comparison. No launcher, service or canonical data changed.
  Repeating this inspection after the NAS probe produced the same three byte
  fingerprints and both checkpoint/recovery fingerprints. Original, backup and
  restored bytes still match. Only the isolated synthetic probe changed.
- The user explicitly approved deploying, committing and pushing the application
  protection first once all code tests pass, while recording NAS direct-write
  hardening as incomplete. No further NAS permissions change is included. A new
  exact-process cutover script preserves the prior launcher, verifies every staged
  file, refuses incomplete test coverage, requires that deferred-NAS approval,
  checks streamed canonical bytes around recovery/startup, and recovers only the
  identified dead writer. Independent scoped review reported no P1/P2. Its
  read-only preflight passed for the eleven-feature configuration and unchanged
  writer/checkpoint identities; activation and live MCP acceptance remain pending.

Preflight: main tracked tree clean; seven existing untracked research documents preserved. Baseline at previous deployed d0846527 (recheck revision before deployment). Node reports approximately 3.9 GiB free memory. Implementation evidence will be recorded below as each gate actually passes.

- Original filesystem guard/capture tests: 6 failing assertions before implementation; original/scope/resource targeted group 20 passed. Binary caller-buffer mutation regression failed before ownership copy; original+document authority targeted group now 17 passed.
- Pure document authority tests: 8 failed before implementation, now pass for local-only confidentiality, derivative inheritance, company/department/room separation, audience intersections, policy revocation, malformed/cyclic policy. Host and all-adapter integration remain pending; this is not a live security claim.
- Staffing sidecar reports 114 passing tests and separate spec/quality reviews passed after stale remote coverage, explicit review, preference and reviewer-reservation corrections. Main integration and final regression remain pending.
- Confidentiality: raw/parsed/export and REST rejection, ambient service-context inheritance, shallow-frozen rule revocation, mid-I/O and pre-response revocation have passed as a 19-test targeted group. New revocation tests first failed on the outgoing response, shallow-frozen rule mutation and awaited physical read. Protected on-disk metadata, inheritance persistence and host-private execution/cache integration still pending.
- Protected Markdown metadata/default loading and signed source preservation: 7 new failures reproduced before fixes; authority/adapter/import group 34 passed afterward. Policy corruption/disappearance and mid-read NAS policy edits fail closed; signed existing originals cannot be replaced, tombstoned or rewritten even with equal bytes.
- Broader original/Wiki/resource/global-sync/scope group: 128 passed, one 5-second timeout in the long curation/synthesis/continuity scenario. Its unchanged standalone rerun passed in 1.28 seconds without increasing the timeout. Full-run stability remains a final gate, not assumed resolved.
- Host feature pure configuration/mapping sidecar: 89 failed before implementation, 89 passed afterward; independent spec and quality review passed. Server assembly/dispatch/discovery/configuration integration still pending.
- Private document-cache admission: three failing tests reproduced before the fix; private ACL/provisioning, warm-cache revocation and prior cache regression group passed 12 tests. Private-cache errors never become a public fallback.
- Streaming revalidation: original 5 behavioral failures and a further 4 mutable-bundle-snapshot failures reproduced. The member and manifest now have frozen private pins; exact hashes, generation checks and authorization remain enforced using fixed 64 KiB scratch buffers. Resource suites passed 36 tests; separate spec and quality review passed.
- Feature integration: default Wiki core, explicit document convenience, direct MCP/read-alias and REST rejection, reactivation preserving files, independent personal/work selection, and Wiki-only pulse tests passed. Existing full-application test imports now use an explicit current-feature fixture, not a production/test-mode default override. Startup-only private feature file and CLI suite passed 26 tests; broader feature/core/document-security group passed 64 tests. Build passed at this intermediate point; subsequent edits need a fresh build.
- Shared-index protection: reproduced a protected source being read by eager shared-index initialization despite output filtering. Shared indexes now wait for policy readiness and exclude protected sources from shared snapshots/embedding admission; authorized document search includes protected Markdown through its separately private document index. Shared-index/authority/feature/enterprise-selection group passed 21 tests. Historical snapshot cleanup and further policy-change races remain under review.
- Owner activity pure policy: behavioral RED 48 failures, GREEN 51 tests, spec and quality review passed. Positive two-prefix/one-grant regression passed (52 total). Runtime candidate/read/execute integration is in progress; directory-only traversal and enterprise/ordinary pulse revalidation remain under review. Host file and live permission state remain pending.
- Staffing integration: five behavioral failures reproduced before host callback/profile/schema/routing changes. General deterministic bookkeeping skips profile loading; remote fallback is forbidden; independent review remains required for high-risk work. Generation-drift, newly added security task, and actionable-knowledge false-positive regressions were each reproduced and fixed. Three-file group passed 206 tests before the last two additions; latest focused group passed seven. Separate spec and quality review passed; two minor test-hardening suggestions remain. This remains routing advice, not actual model or code execution.
- Registration: personal/enterprise purpose must match host authority; department claims are checked against administrator-verified membership without changing it. Three new failing tests reproduced; enterprise authentication group passed 11. Verified navigation projections and broader integration review remain pending.
- Host-only snapshot storage: new bounded namespace helper passed three real filesystem/Windows ACL tests. No configuration means no disk persistence; unsafe host locations/names and oversized replacements are rejected. Shared metadata/lexical/semantic adoption and legacy advisory retirement remain in progress, not deployed.
- Shared host-cache integration: metadata and lexical snapshots plus semantic manifests, pending work and database directory now use the verified private host namespace. No configured host storage leaves lexical/metadata memory-only and semantic indexing unavailable. The five-file host/semantic group passed 61 of 62 tests; its MCP locator fixture lacked the new host configuration. The corrected locator fixture and two new community persistence tests passed together (3 tests). Public discovery and reaction snapshots now use bounded host storage too; their NAS-write regression failed twice before the change. Broader migrated-test regression is still pending.
- Shared-index authority races: reproduced semantic source revocation after read, during model initialization, during batch fallback, during excerpt hydration, stale status counts and warm lexical hydration. Targeted model-call/cache tests passed after admission checks; the host/semantic group above includes the hydration fix. No paid or real embedding model was invoked by these regressions.
- Owner runtime/catalog implementer reported 99 passing targeted tests. Independent spec review then found action downgrade through raw operation labels, missing ledger path/write hooks, Windows physical-path normalization, nested owner-context intersection, disabled mixed-operation projection and final control-response/cursor freshness gaps. These are open correction items, not completed authorization guarantees. No deployment occurred on this evidence.
- Snapshot migration regression: seven-file metadata/lexical/filesystem/consumer group passed 315 tests, skipped one, and timed out one real-ACL restore/rebuild test at its old five-second ceiling. The explicit Windows-IO timeout was adjusted only for that case; the complete six-test consumer file subsequently passed. Warm sorted metadata and count revocation were separately reproduced and corrected.
- Document cache concurrency/persistence: eight concurrent reads initially parsed eight times; now one exact-generation parse is shared while all eight caller/source validations remain. Disk publication blocking, nine queued generations, every-save directory enumeration, and public cache ACL omission were reproduced before corrections. Optional saves are nonblocking, bounded to four queued generations and 32 MiB conservative metadata estimates; no original raw buffer is retained by the persistence queue. A bounded in-memory disk ledger replaces per-save directory scans, with periodic reconciliation and at most 16 cleanup attempts. Restored confidential-file privacy failure was also reproduced and fixed. Six document/original-streaming suites passed 50 tests. Independent review and the broader process-wide active-memory budget remain pending.
- Cache-disk follow-up: shared-writer and revoked queued-source tests failed before an exclusive private-root writer lease and publication-time admission checks. Further independent review reproduced mutable principal substitution, actual source-ancestor junction substitution, and post-rename capacity undercounting; all were corrected. Principal/nested membership copies, canonical path reauthorization, uncertain-write reconciliation, abandoned/replaced lock preservation and foreign-file preservation under actual eviction now have passing regressions. Scoped independent SPEC rereview passed; fresh QUALITY review is pending. Abandoned locks are never stolen and disable optional writes until host inspection; originals remain readable. Cooperating writers must respect the lease; external host changes are detected by bounded periodic reconciliation.
- Work-memory admission: source-read/parse/concurrent-overbooking tests initially failed (3); a further blocked-persistence accounting test failed before queued metadata/serialization/compression reservations. Seven document suites then passed 67 tests. Streaming hash scratch-buffer admission was separately reproduced and fixed; resource/work-memory suites passed 24 tests. The shared budget is conservative admission accounting, not an OS/native-allocation hard limit. Restored-cache/PDF accounting, request snapshot reuse and broader search optimization remain in progress.
- Owner control/economy rereview: implementer reported 66 passing tests, but independent SPEC rereview still found final dispatch control-response freshness, durable economy intent before admission, commit-time consent refresh, and explicit mixed-listing action classification gaps. Corrections and stronger recovery/positive-write regressions are in progress; no owner integration completion claim or deployment follows from the test count alone.
- Cache-disk QUALITY review passed after partial-write ownership, pre-byte privacy and final awaited-authority checks were corrected. Source and storage revocation immediately before native semantic mutations also have passing regressions. These scoped approvals do not cover later memory/ranking changes or deployment.
- Request work memory: five new failures reproduced duplicate decoding, operation generation drift, unbudgeted PDF hot retention, leaked reservations after file-close failure and silent empty search on budget exhaustion. All five passed after corrections. The six-file regression group passed 67 tests and failed two search fixtures: one real-filesystem multi-window timeout and one synthetic byte counter inconsistent with exact-hash validation. A scoped timeout and real-snapshot validation behind that accounting fixture corrected both; search/work-memory then passed 17 tests.
- Independent work-memory SPEC review identified pre-parser YAML and wide-GFM-table expansion gaps. Two smaller synthetic reproductions failed before additional frontmatter complexity admission and table-cell punctuation accounting; the four-file work-memory/resource/concurrency/search group then passed all 42 tests. SPEC rereview and fresh QUALITY review remain pending. This does not establish measured process peak memory or native-worker coverage.
- Owner implementer reports latest six-file 76-test pass and returned the serialized test slot. Independent rereview of the four prior findings remains pending; no NAS deployment or commit/push has occurred.
- Work-memory SPEC rereview passed after YAML/GFM corrections. Fresh QUALITY review then reproduced short-read backing-buffer over-retention and parent PDF-output retention after unconfirmed exit. Both new tests failed for the expected reasons. The initial reader now fills one admitted buffer, and PDF failure disposes output collectors before rejection; GREEN verification is pending because available RAM fell below the user's 3 GiB minimum.
- Bounded ranking: two pure top-k tests first failed and then passed, including exact agreement with exhaustive sorting over 10,000 deterministic rows. A document-search regression now passes with only 100 retained ranking candidates. Signed rank anchors and window-aware metadata pagination are implemented but broader regression is pending. Body-free revision-pin and cursor-reuse tests exist but those features are not yet implemented; the worktree intentionally does not currently have an all-green test suite.
- Owner late-revocation follow-up: implementer reports four focused passes after final file-backed refresh, real exact-path grant writing and pending-intent rollback. Independent rereview confirms the first two but finds one remaining P1: rollback's lock/checkpoint reads still inherit the revoked owner freshness guard, so the cleanup can fail and leave replayable intent. A production-ambient regression and narrowly fenced abort cleanup remain required.
- Execution blocker (2026-09-12 03:35 KST): repeated RAM preflights observed 2.98, 2.51 and 2.72 GiB free. Heavy jobs were not started below 3 GiB. A read-only process inspection found no remaining Vitest/npm job; most Node processes belong to Codex, and none were terminated. Latest full build/test/deployment/staging/commit/push gates remain outstanding. Existing originals, NAS state and seven unrelated research documents remain untouched.
- Final pre-pause check: available RAM fell further to 2.34 GiB; HEAD remains `d0846527`. The memory QUALITY reviewer accepts both source corrections on inspection, explicitly without GREEN evidence. No further heavy command was started. The latest `git diff --check` returned exit 0 (line-ending warnings only).
- Owner handoff is frozen: a realistic, unrun regression in `src/economy-ledger.test.ts` now enables document policy and makes ambient freshness reject after owner revocation. No production fix was added for this final P1. All implementation/review workers have stopped; no test or build is left running.
- Resumed on the user's request after RAM recovered above 4 GiB; heavyweight preflight now requires 3.5 GiB to leave headroom for the 3 GiB floor. Work-memory short-read/PDF lifecycle corrections passed the focused 16-test group. Owner ambient-revocation recovery and unauthorized ledger initialization regressions failed before narrowly captured host cleanup and abort-only guarded intent recovery; seven owner/endpoint/storage suites then passed 85 tests. Independent SPEC review found a missing final journal fencing check; its nonce-substitution reproduction failed before adding final lock checks at both journal publication sites. Economy/storage suites passed 28 tests, and the guarded no-journal restart test passed. Independent rereview is pending.
- Body-free opaque member/manifest pins: two behavioral failures reproduced before implementation; resource/work-memory/PDF suites passed 46 tests. Search next-page full-document loading failed again on resumption, then passed with a maximum of eight budgeted, 100-row metadata windows and exact revision-pin validation. The next three-suite search/authority/resource run passed 42 tests and failed one assertion that omitted the new, correct stale-revision error wording; this assertion was broadened to accept revision errors. Fresh GREEN and independent review remain required; no deployment, staging, commit or push has occurred.
- Search/authority/resource rerun passed all 43 tests. Incremental resource classification was reproduced at 2,000 redundant classifications over 1,000 non-document paths on a warm search; the correction performs zero repeated classifications, classifies one newly added path once, and still rejects revoked visibility. Search suite passed 12 tests; a shutdown cleanup regression failed before wiring the cache lifecycle and then passed. Intermediate TypeScript build passed at 04:07 KST.
- Owner/economy SPEC passed; fresh QUALITY then found final-callback physical path substitution and mixed-read endpoint summary regressions. Real temporary-directory tests reproduced an out-of-root journal replacement and redirected cleanup deletion; final synchronous confinement/identity fences and owned-temp cleanup corrected these. Active mixed-read discovery failed before deriving its summary from individually authorized operations. Four suites passed 56 tests after correcting the explicit owner/host test fixture, and independent QUALITY rereview passed. These checks do not provide an OS-administrator or physical-storage guarantee.
- Independent search SPEC found three P2s: in-flight post-shutdown cache publication, collation-equivalent path anchors after eviction, and valid Unicode cursors exceeding their input bound. All three failed behavioral tests before lifecycle checks, exact-string tie-breaking and a shared 4,096-character search cursor bound. Search/document-adapter suites passed 17 tests. SPEC rereview is pending. A separate indexed-situation regression now fails because the old path still enumerates unrelated metadata; that optimization remains unimplemented, so the full suite is not yet an all-green completion gate.
- Search SPEC rereview passed. Fresh QUALITY review identified two P2s: retained substring backing storage in page descriptors and failed candidates lacking generation pins. The repaired-invalid-UTF8 candidate reproduction failed; windows with failed/unadmitted candidates now decline caching, but its GREEN run remains pending. A synthetic isolated V8 string-storage test is prepared and unrun; string detachment is not yet implemented. The reviewer is closed and no independent approval covers these later edits.
- Situation metadata: the full-enumeration reproduction and a separate 40-versus-1 lexical admission-call regression both failed before changes. The existing metadata index now maintains derived eligibility sets and queries existing literal-condition postings; ordinary candidates use set membership and lazy revisions instead of a complete metadata/revision walk. Lexical posting discovery precedes caller admission, which still precedes the result limit. All seven situation-selection tests passed in the 04:23 KST three-file run. An additional indexed/fallback parity, revocation and drift test is prepared but unrun. Semantic lazy-revision checks are not implemented yet; two prepared tests require a corrected explicit host-private fixture before their RED result is meaningful.
- The same three-file run passed 39 tests and failed 9: the reproduced failed-candidate cursor bug, one synthetic lexical fixture missing its path-posting membership, and seven semantic cases whose old fixture lacked the newly required private host cache. Fixture corrections are prepared, not verified; no passing semantic gate is claimed. During the run, free RAM dropped from a 3.946 GiB preflight to 2.937 GiB by completion. No test process remains running. To preserve the user's 3 GiB minimum with observed headroom, subsequent heavyweight starts require 4.5 GiB free; repeated later checks remained approximately 3.2–3.45 GiB. Latest diff check passed. Build/full regression, remaining plan items, NAS deployment and commit/push are still outstanding; no NAS or Git publication was performed.
- RAM subsequently recovered to 4.637 GiB. The V8 reference-storage test failed with an actual `SLICED_ONE_BYTE_STRING_TYPE` cached reference, and both semantic lazy-revision tests failed for their intended stale-result reasons after fixture correction. Cloning page rows before retention and semantic revision checks before candidate groups and after awaits corrected these. At 04:29–04:30 KST, the full document-search, context-selection and memory-retrieval group passed 50 tests. A fresh TypeScript build passed at approximately 04:32 KST. Search QUALITY rereview passed both corrections; that reviewer is closed.
- Situation SPEC review then found two P2s not covered by that passing group: flattened literal dotted keys can falsely activate ordinary notes, and lazy lexical revision checks cannot detect missing/stale rows excluded by old postings. Four new regression cases cover dotted keys, missing rows, stale postings and rehydrated text with old postings; these are prepared but not run or fixed. The reviewer confirmed there is no existing common revision-coverage barrier and is closed. No situation QUALITY approval exists yet.
- Selective LlmWiki invalidation remains unimplemented. Four new tests cover unrelated non-wiki edits, scope-dependent lint/summary invalidation, current permission revocation and conservative reset/dependency invalidation. They are prepared but unrun. Later preflights fell from approximately 4.15 GiB to 3.352 GiB, so no new heavyweight run started. There are no running child tests/builds or reviewers. The worktree is explicitly not yet an all-green or deployable completion gate, and NAS deployment, staging, commit and push remain untouched.
- Resumed after the user approved smaller test runs. The former 4.5 GiB start threshold was a conservative agent choice, not a product requirement. Single-file runs now start with at least 3.25 GiB available and sample free RAM every 100 ms; the wrapper stops only its own test process tree if availability drops below 3.125 GiB. This is sampling with headroom, not a guarantee against unrelated applications allocating between samples. Completed runs observed minima of approximately 3.16–3.55 GiB; no unrelated process was terminated.
- The dotted-key activation regression failed, then passed after checking actual nested positive conditions. Missing, stale and rehydrated-but-old lexical postings each falsely reported complete results before the new revision-coverage barrier. Coverage reconciles revision-only metadata once per pair of index generations and memoizes only discrepancy paths; warm calls reuse that check while applying current caller/condition admission to discrepancies. Posting revisions remain separate from hydrated-text revisions. All 12 situation tests passed. Independent rereview then found a cache unregister/reset gap; its reproduction failed before explicit WeakMap resets on query invalidation and close. The expanded 14-test situation suite passed, including warm revision-call counts, different index identities, current permissions/conditions and budget eviction. SPEC rereview passed; fresh QUALITY review is pending.
- Selective LlmWiki invalidation now retains summaries across unrelated non-wiki edits after checking only changed metadata, invalidates known contributors and possible promotions, and retains private-scope-disjoint lint/summary entries. Unknown resets remain conservative; in-flight work is not republished after invalidation. Three regression tests failed before implementation, then the six-test suite passed with current revocation, department membership and concurrent-edit checks. This scoped implementation is awaiting independent SPEC review and a later fresh QUALITY review. No fresh full build/test gate or NAS/Git publication has occurred since these changes.
- Department navigation: after correcting the test endpoint to the existing `mcp.query_notes`, four behavioral failures reproduced absent guidance, unfiltered counts/pages, cursor drift and self-claimed membership. Verified default navigation now intersects protected department policy with current access. Protected sources remain excluded from shared indexes, so this explicit query uses fresh request-local metadata, bounded at 64 KiB/source; it is not claimed as an indexed speedup. Four tests passed, including cross-department access, confidential exclusion, 512-character packing and changed membership. The same run passed all 14 situation tests after closing the fixture's search watcher before teardown. Independent review is pending.
- Selective-summary SPEC review found computation/delivery races beyond cache publication. Four new failures reproduced generation, ACL and policy drift in producer/shared-promise responses and invalidation after awaited cache-hit validation. Final delivery guards now cover each route, including uncached catalogs. All 10 selective-invalidation tests passed; independent SPEC rereview and subsequent QUALITY remain pending. Latest observed run minima were 3.88–4.63 GiB free RAM.
- Deployment preparation is read-only: the scheduled NAS-backed service still runs the isolated `20260911-axis-evolution/release` build; HEAD is `d0846527`, origin is the user's fork. NAS root inspection reported an Everyone FullControl ACE and no top-level `_sources` directory. This is not an operational immutable-storage guarantee. No NAS ACL/data, launcher, deployment, staging, commit or push was changed. A scoped worker is preparing the synthetic comparison harness and owns the single heavy-test slot; main is preparing documentation and reading deployment boundaries.
- A bounded NAS directory inspection subsequently found `Community/_sources`: five originals, 120,134 bytes. New host-only `PreservationV1` storage was provisioned with inheritance disabled and only the current user, SYSTEM and Administrators allowed. All eleven current features were explicitly selected in its startup configuration; no provider/execution/owner grants were added. Originals were copied to a private backup, then restored from that backup to a separate private verification folder. Source-before/source-after/backup/restored manifest hashes all matched (`C5E02E254E906D850881EA56DAF0F347A5FBCB285E0E9EEC8AD0B735CA984973`); all 24 new host-tree paths passed ACL/link checks. No original bytes, NAS permissions or running service were changed. Existing NAS source ACLs still allow write access, so operational prevention of direct SMB changes is not established.
- Situation QUALITY passed after fixture lifecycle cleanup. LlmWiki SPEC passed after inspecting the delivery guard refactor; its fresh test rerun remains pending. Department SPEC passed for the explicit default-selector scope (ordinary queries retain their existing protected-document limitations). A fresh independent reviewer is now reviewing LlmWiki and department QUALITY. Synthetic benchmark implementation is in progress; full build/regression and deployment/publication gates remain outstanding.
- Selective-cache and department QUALITY review reproduced five P2 regressions: Windows case-folded invalidation, unbounded per-caller retained caches, undercharged collection Map contents, protected-source discovery exhausting the request observation budget, and exact totals shrinking on cursor pages. Each was corrected after a failing regression; the latest combined suites passed 22 tests, and independent QUALITY rereview passed both areas. Physical metadata reads still record protected-source dependencies; only ACL-checked discovery defers observation. Separate lookahead/filtered-row assertions remain a coverage gap, not a demonstrated defect.
- Fresh build attempts were interrupted by the owned-process RAM guard, first at a sampled 2.822 GiB and then at 3.098 GiB after increasing stop headroom. No unrelated processes were stopped. The first dip demonstrates that the sampler cannot guarantee a floor between checks. These interrupted builds are not passing verification; generated output still requires a successful complete rebuild. Benchmark SPEC added eight genuine failing cases for returned private paths and unsafe temporary-root admission; corrections are in progress.
- Build recovery: the installed TypeScript 7 Node shim invokes a native compiler, so a Node heap limit did not constrain compilation. Its locally documented `--singleThreaded` option completed the unchanged build project successfully (exit 0, sampled minimum 3.305 GiB). `git diff --check` passed. The compiled startup loader then validated the external eleven-feature configuration and both private cache roots against the actual NAS Vault, read-only. The single-worker full suite is now running; neither this build nor configuration validation is a live deployment claim.
- Full-suite attempt was interrupted, not passed. Its original RAM wrapper ignored a failed tree-termination result and kept running after the threshold; the final sampled minimum was 2.568 GiB. Exact parent IDs, command roles and creation timestamps were checked before an elevated stop of that owned Vitest tree. A subsequent process check confirmed the runner and all reported descendants were gone; no unrelated process or service was stopped. Further supervisors check termination status and use a direct owned-child fallback, with 3.5 GiB stop headroom and 50 ms sampling. This is still sampled protection, not a guaranteed OS memory cap.
- Benchmark SPEC and separate fresh QUALITY both passed on source. All eight SPEC2 regressions then passed, followed by all 13 Node tests including the real Windows drive probe and two-process 100-document smoke (36 query quality gates, both stale reads rejected, minimum sampled 3.684 GiB). The 1,000-document run also passed all 36 query gates and stale probes; its raw synthetic report is retained under `docs/research`. The 10,000-document run is pending. Model tokens, model answer quality and NAS traffic remain unmeasured; these local results are not a claim of lower monetary cost.
- The 10,000-document attempt first stopped at 3.456 GiB; the confirmed process-tree stop left its synthetic fixture for later inspection. After RAM recovered, a fresh 10,000 run passed all 36 query gates and both stale probes, with a sampled minimum 4.206 GiB. Raw 1,000/10,000 reports and the comparison table retain initialization, maintenance, process memory, serialized-character counts and measurement limits. Indexed retrieval read fewer logical source bytes but used more peak worker memory and returned larger evidence envelopes; no model-token or monetary saving is claimed.
- The first four of 32 diagnostic test shards exercised 674 tests: 667 passed, one skipped and six failed. Five failures were obsolete catalog expectations (registered versus active count, missing explicit owner consent, and host-unconfigured availability); corrected expectations retain active-only and fail-closed behavior, and the complete 11-test capability suite passed. The remaining real HTTP/Windows-ACL roleplay scenario reproduced its five-second timeout twice; with all assertions retained and a scoped 30-second scenario timeout, it passed in 7.374 seconds. The production code was not relaxed. A new complete `npm test -- --maxWorkers=1` run is in progress; early shard results are not the final all-green gate.
- Deployment artifacts were prepared, not activated: 861 generated files were copied into a new isolated preservation release with zero hash mismatches; the prior launcher was preserved and its live hash stayed unchanged. A parsed next-launcher draft adds only the explicit feature configuration and two private cache roots. The NAS login page identified NAS2dual, and the user subsequently logged in. Browser control failed at kernel asset initialization even after retry/reset, so no NAS management mutation was performed; screenshot-guided inspection is continuing. NAS operational source protection remains unresolved.
- The 06:13 full run was stopped around 06:44 KST by the RAM guard, with a sampled minimum 3.495 GiB and successful owned-tree termination. A process check found no remaining matching supervisor/runner. Its final JSON report was not produced; partial passing output is not complete regression evidence. Failures found include catalog generation fencing during ordinary integration reads, skill-directory trailing-separator admission, long ordinary-note paths, remote federation identifiers interpreted as paths, older owner-consent/private-cache fixtures, session-revocation error expectations and a federation timeout. These require reproduction/correction; a read-only investigator is examining only the catalog family. The next complete gate will use fixed-basis sequential shards so completed coverage survives a RAM stop without mixing code revisions.
- Read-only NAS reconciliation found that the operating share was custom-configured during the prior cutover and is not listed in the vendor SMB UI. The recorded physical folder is `/VOL1/Data/llm_wiki/llm_wiki-nas-20260909`; its five original files (120,134 bytes) exactly match the current MCPVault share by relative path, size and SHA-256. The user-confirmed `_sources` properties show no per-folder permission controls. No Data-wide permission, source property or NAS setting was changed. The user authorized preparing a separate administrator-shell connection for read-only permission-support inspection. Port 22 gave no response; port 23 accepted then closed without a banner. The vendor UI explicitly shows Telnet stopped, with no SSH item on the inspected settings page. Enabling a management service, authentication, and permission changes have not occurred.
