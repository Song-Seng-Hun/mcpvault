# Arkon-inspired compilation: approved sequential execution

## Authority and baseline

User approved three bundles: (1) enterprise processing policy and durable jobs,
(2) source-linked fidelity and bounded knowledge application, (3) Codex hooks and
community integration. This implements mechanisms independently; no Arkon code
is copied. Its company-global scope is not MCPVault's public Global.

P1/P2/P3 and graph-contract phase1 were delivered before this work. Handoff was
confirmed with `llm_wiki2`; baseline is main `50c0b2b85251d833ef43647805a2cacf68035ae1`.
Seven pre-existing untracked research documents belong to others and are excluded.
This task owns compilation modules and minimal service/endpoint connections,
related tests/docs/dist, and the single verification/Git/NAS slot. Existing graph
contract modules are read-only dependencies; subsequent graph work is coordinated
after bundle1 delivery.

## Bundle1 verification record

- Latest targeted run: five files,103 passing tests and one existing skip.
  Separate CLI/registry/feature coverage previously passed135 tests in four files.
- Strict production build passed after the latest source changes, using the
  guarded TypeScript command equivalent to the package build script.
- Independent SPEC and QUALITY reviews completed. Recovery review added a
  twelve-case deletion/rollback matrix spanning notification, adapter absence,
  retry and fresh request IDs. Applied proof now survives status changes and
  completion-receipt failure. These regressions failed before the fix and pass
  after it; uncertain pre-commit retries remain supported.
- Current full460-file run is pinned to code/test/build basis
  `5d4ea025324de1de7cc68c30e861808348c3e5c038d85921d8adb0e726af7ada`.
  Earlier runs were intentionally stopped for review fixes and are not counted
  as passing evidence. The first completed run had6462 passes, four existing
  skips and one beforeEach timeout in the existing semantic-reuse native fixture.
  Its unchanged27-test file passed separately; temporary setup timing did not
  reproduce the timeout and was removed. No deadline or assertion was relaxed.
  The fresh full run of the same source basis passed on2026-09-13:460 files,
  6463 passing tests, zero failures and four existing skips. Minimum free memory
  was3.116GiB with the single512MiB worker and512MiB coordinator.
- Owned staged paths and whitespace checks passed; content review excludes host
  settings, credentials, recovery data and unrelated research documents.
- Local deployment/rollback copies and read-only preflight passed:906 dist
  files match tested output, and canonical original/backup/restoration plus
  world/economy preservation checks passed. Live activation and read-only
  acceptance subsequently passed as recorded below; Git delivery follows.

## Required boundaries

- Current access, verified processing runtime, and source-only/synthesis mode
  are separate checks. Host configuration absent means diagnostic-only.
- Unknown classifications do not authorize public capture. Existing documents
  are not automatically enrolled or reclassified. Administrator/model names and
  maintenance grants are not compilation grants.
- Apply restrictions before any derived body. Inherit all original restrictions.
  No provider fallback, transcript harvesting, new inference service, autonomous
  model process, or automatic confidentiality relaxation.
- Pin exact revisions, explicit members/relations/concepts, rule version,
  authority, output revision, and last managed output fingerprint. Human changes
  or unmanaged existing output require review, never overwrite.
- Generation, checking, application and completion receipts are distinct. Reread
  and durable completion receipt are required. Resume validates applied content,
  never reapplies an old patch over changed output. Corrupt history is preserved.
- One worker/document, stable request IDs, three failures maximum, bounded queues
  and responses. Unchanged events do not generate repeated actions.
- Markdown/revisions remain authoritative; graph vocabulary is reused from
  `graph-contract.ts`, not redefined. Fixed MCP tools remain five.

## Delivery checklist

### Bundle1 — enterprise policy and resumable job foundation

- [x] Explicit ownership handoff and live checkout baseline inspected.
- [x] Separate host grant and verified private job storage.
- [x] Policy admission and execution gates, current authority invalidation.
- [x] Pinned durable state machine, conflict detection and crash recovery.
- [x] Dynamic service entry point and bounded diagnostic projections.
- [x] Targeted RED/GREEN, strict build, full regression, independent SPEC/QUALITY.
- [x] Staged paths/content review, rollback-preserving NAS deployment and live checks.
- [x] Source plus dist committed/pushed as `c670fe9e1672991fc7d821734fa450e542eeda19`
  to existing user-fork main; remote verified again on2026-09-13.

### Bundle2 — evidence and automatic knowledge

- [ ] Required facts with revision/locator and separate semantic judgment.
- [ ] Deterministic literals checker: match/suspect/out-of-scope/unavailable.
- [ ] Existing source compare/publication/change-set integration, one refinement.
- [ ] Frozen diagnostic/evaluation fixtures; old80 queries untouched.
- [ ] Same verification/deployment/fork delivery gates as bundle1.

### Bundle3 — host hooks and community

- [ ] Installed Codex event support verified without user data.
- [ ] Bounded event bridge, Plan mode diagnostic-only, no transcript scanning.
- [ ] Existing consent, participation receipts and heartbeat reused; no new wakeups.
- [ ] Quota/cancellation/reentrancy/duplicate-public-action tests.
- [ ] Same delivery gates. Enable only actually authorized/verified configuration.

## Acceptance and operations

Use the inherited one-worker verification supervisor, admission2.3GiB/reserve2GiB
(explicit user approval); never close user applications. Each bundle runs targeted
tests, build, full tests, independent review, diff/staged/secret checks, NAS-backed
deployment with rollback and live verification, then existing-branch commit/push.
No upstream contribution, PR, package/release publication, new branch or worktree.
Originals, world/economy and credentials are preserved. Telnet stays off; NAS
direct-write protection is outside scope.

Quality gates remain per-language Recall@5/MRR non-regression, required evidence
coverage +5pp at4000/12000 characters, and no extra negative false positives.
Unit checks do not establish model quality; incomplete evaluation never passes.
Automatic knowledge application stays disabled until its quality and host gates
are verified. Unchecked bundles are not shipped implementation.

### Bundle1 NAS acceptance — 2026-09-13 KST

The tested release was activated through the existing NAS-backed service launcher,
preserving the prior release and launcher for rollback. Canonical original bytes
and world/economy preservation checks passed. Read-only MCP acceptance against
that service passed: exactly five tools, orientation and its primary action,
dynamic compilation discovery and a 67-character diagnostic response. With no
compilation host configuration, the response remains `diagnostic_only` with
`host_configuration_required`; automatic application is disabled. Acceptance
created no Wiki documents and performed no content mutations. Telnet was not used.

### Bundle2 diagnostic checkpoint — 2026-09-13 KST

This is an intermediate implementation, not completion of bundle2 or bundle3.
The user's latest instruction is solo work: do not message/wake `llm_wiki2` or
start its graph handoff before this task is complete. Its untracked graph files
and the seven unrelated research documents remain excluded from this delivery.

Implemented: read-only `wiki.fidelity_check`, bounded revision/locator reports,
separate agent semantic judgments, local number/date/version/quote checks,
one refinement preserving required source anchors, deterministic prepared
publication through the existing guarded writer, and a host-only publication
adapter. The adapter is not selected or enabled by the deployed server. Missing
host grants still mean diagnosis only. No provider, model, hook or scheduler was
started; no original conversion or live content mutation is authorized here.

Recovery checks cover real publication acknowledgement loss, manual edits after
interruption, manual target creation after preview, unchanged receipt writes and
revocation after restart. Follow-up reads preserve complete verified source spans;
invalid locators cannot create nonexistent line ranges. Targeted verification:
eight files,243 passed. Production build passed with guarded single-threaded tsc.
Full regression passed:466files,6571passed,zero failures,four existing conditional
skips. Immutable source/test/build basis:
`f3e2609770d7abbb370900e3ba0eedc2c4304375c6a2944e1df32004363d0d3e`.
Minimum free memory was4.348GiB. The full checkout test set includes the peer's
untracked pure graph tests; its source and generated artifacts are not delivered
in this checkpoint. NAS activation and read-only live acceptance passed using
the same924-file build. Original/world/economy bytes and prior release/launcher
were preserved. Live MCP verified fixed5, compilation diagnosis, read-only
fidelity discovery and invalid-input non-disclosure. Automatic application is
false; no operational documents were created. Staged path/whitespace/content
checks passed, excluding host data, credentials and unrelated files. This
checkpoint's source-plus-dist commit/push follows these completed delivery gates.

The 24-case synthetic diagnostic fixture is distinct from the untouched frozen80
retrieval evaluation. Four inconsistent labels/bodies in an initial research
fixture were corrected to their original no-literal texts; the invalid initial
run is not evaluation evidence. Reviewed fixture SHA256:
`84c2d7555b78c34c9ada9f5d8eecae623841d690a2614c2d048cef36d6bdcc9f`.
No actual-model +5pp quality claim is made. No independent bundle2 review was
recovered after the desktop restart; solo inspection is not independent review.
In response to the explicit question about substituting solo review for this
diagnostic checkpoint, the user instructed continued execution without further
interruptions. This checkpoint uses that exception with automatic application
disabled; it is not an independent-review claim or automatic-synthesis approval.

Remaining: explicit host/runtime activation and quality gates, source-only and
already-covered no-write receipts, omission/permission/manual-edit projections
in answer packets and the exception board, complete rationale/checkpoint flow,
actual-model evaluation, and all bundle3 hook/community implementation and event
verification. Automatic knowledge application remains disabled. This checkpoint
must not mark those requirements completed or start the graph handoff early.
