---
id: reviewed-skill-metadata-wave13
description: Ten metadata reviews distinguish advertised hooks and examples from executable permission.
keywords: [metadata, hooks, GitHub, cancellation, camera, card-game, 실행-예시]
use_when: Reviewing source completeness and potential effects before preparing limited procedures.
parent: 2026-09-15-reviewed-skill-release.md
previous: 2026-09-16-reviewed-skill-metadata-wave12.md
next: 2026-09-16-reviewed-skill-metadata-wave14.md
---
# Examples are not execution grants

Main read all 15 retained files: 24,414 bytes across ten inventory entries.
Bounded capture and final double NAS fingerprint checks matched the captured sources.
These are point-in-time checks, not an atomic NAS snapshot or permanent freshness guarantee.

| Entry | Reviewed value | Limitation or distinction |
| --- | --- | --- |
| block-no-verify-hook | Commit/push bypass prevention intent | no-verify commands are blocked counterexamples; handler absent |
| brainstorm | Game concept ideation and review modes | Runner and complete procedure absent; no agent grant |
| bug-report | Describe/analyze/verify/close modes | Closing is a mutation, not proof of a fix |
| bug-triage | Bug scope, sprint and trend hints | Ranking/update logic absent; count threshold is not authority |
| builtin-generative-ui | HTML artifact and embed conventions | Imported CSP claim is not current-host isolation proof |
| builtin-migrate-workflows | Workflow-to-skill migration intent | Implementation, safe archive and rollback proof absent |
| builtin-permissioned-github | Explicit gh repository scoping | -R does not prove login or authorize remote mutations |
| camera-systems | Follow/framing, smoothing and limits | Small-map branch is only a comment; compatibility untested |
| cancel | Stop named OMG work modes | No runner/ownership proof; cannot stop unrelated processes |
| card-game | Effect data, ordering and replay design | Unbounded resolver loop; atomicity/validation not implemented |

The three builtin Apache notices contain only a preamble; complete rights provenance is unresolved.
Release/version claims remain source claims, not verified installed APIs.
Camera shake hooks are not host hooks; card destroy operations are not disk deletion.
Card resource costs are not financial transactions; game code was not executed.
Seven potential-impact axes preserve worst-case conditional reach, not a maliciousness verdict.
Usage and co-use remain unknown; no fabricated counts or inferred merge decisions.

## Verification and delivery boundary

Ten field acceptance checks and 80 negative controls pass against unchanged captured bytes.
Fifteen contiguous quote ranges and full-file ledgers cover all retained files.
The acceptance test first failed because the ten reviewed descriptors were absent.
Mechanical locator checks verify bytes; metadata meaning remains a separate main-agent review.
Sealed input hashes and all ten final source-recheck records were revalidated after continuation.
Index counts: 77 reviewed with limitations, two fixtures classified, 1,531 pending; total 1,610.
Live usable remains zero. Second index build created nothing; all 1,610 rows were reread.
Fixture exclusions, historical evidence and prior work-index generations remain intact.
Production source/dist unchanged; existing regression-e basis revalidated, not a new suite run:
525 files, 7,074 passed tests, four Windows skips. No runtime redeployment was performed.
No target script, hook, installation, worker, account grant or registry admission was executed.
Previously refused host-access/original-read routes were not retried or bypassed.
Next: continue bounded metadata reviews and useful derivatives; resolve admission separately.
