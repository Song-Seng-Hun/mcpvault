---
id: runtime-diet-continuation
kind: implementation-record
description: Consolidate runtime coverage checks, MCP output projections and isolated test connections.
keywords: [diet, duplication, MCP, fixtures, Luna, 순감소]
use_when: Reviewing the continuation after the validation-workflow release.
position: Diet implementation chapter; byte-ceiling work follows separately.
parent: 2026-09-21-diet.md
previous: 2026-09-21-diet.md
next: 2026-09-21-diet-output-boundary.md
---
# Runtime and fixture diet

Baseline: main 2e7a378ac; retain unrelated edits and research.
One reused Luna owns fixture consolidation and test execution. Main owns output projections and risk review.

## Changes and protected behavior

- Reuse source-coverage checking; retain observation authority and output-access boundaries exactly.
- Reuse ordered snapshot checks; keep the distinct final snapshot-then-authority order.
- Move generic response budgets and note/navigation projections out of createServer.
- Share four prefix searches; preserve final-page checks, revisions, surrogate boundaries and continuations.
- Reuse fresh MCP connections in twelve test files. Keep names, options, cleanup and assertions.
- No tests, public endpoints, source-protection guards or permissions removed.
- Relocate 305 existing guidance source references; templates and bindings unchanged.
- Fifteen uncatalogued IDs already existed at baseline; this refactor does not fix that unrelated drift.

## Accounting

This continuation: handwritten production -20 lines; tests/helpers -65; total -85.
Earlier workflow bundle was +77, so combined diet is -8 lines, excluding prior curation fixes.
createServer alone loses 607 lines; most are moves, not net savings.
Generated guidance/dist and documentation are excluded from functional line savings.
Do not present this small net decrease as a substantial whole-repository reduction.
Model billing, 80% output savings and full-suite speed improvement remain unmeasured.

## Validation and delivery

Run affected tests and build after this source freeze; retain earlier failed/intermediate evidence.
Targets: 34 files / 276 passed; build passed. Initial full run stopped at stale architecture metadata.
Architecture hash refreshed. v2 then hit a 5s pulse timeout; unchanged isolated file passed 45/45.
v3 isolates first 20 files, then uses 20-file batches. Keep timeouts/assertions; no production fix claimed.
Final v3: 566 files; 7,457 passed / 7,461 total; four unchanged Windows skips. Source basis verified.
First-to-last test timestamps: 2,742.1s (45.70 min), not a whole-process or billing measurement.
Staged paths passed. NAS runtime deployed; live search/read/continuation/graph checks passed.
Canonical originals/world/economy bytes preserved; no Vault body writes or new account bindings.
Retain previous release and v1/v2 failure records. Fork commit/push follows this release record.
No new accounts, bindings, branch, PR, upstream publication or live Vault body writes.
5KB transport compliance and broader dead-code/consolidation work remain incomplete.
