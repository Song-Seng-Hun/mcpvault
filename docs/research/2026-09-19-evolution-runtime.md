---
id: evolution-runtime-results-20260919
kind: implementation-report
description: Concrete runtime slice, validation evidence and unresolved operational gates.
keywords: [HumanGrad, TaskGrad, harness, metering, provenance, 진화]
use_when: Checking what is implemented versus actually operating.
parent: ../architecture/evolution/index.md
previous: ../architecture/evolution/runtime-usage.md
next: ../architecture/evolution/execution-2026-09-19.md
---
# Evidence-bound evolution runtime

Baseline: `bf5676a28`, existing `main`, same user fork. No new grants or accounts.
This is an engine slice, not full A/B/C completion; the execution checklist is pre-freeze.
## Implemented

- Concrete `evolutionRuntime` connection uses current existing-account authentication.
- Host-only feedback/delivery/use receipts bind exact revisions and authority.
- Sequential code-owned evaluation runs baseline/candidate and skill-free variants.
- Behavioral/operational selection requires three paired trials and non-regression.
- Cost-only adoption requires at least 10% median saving, not any small difference.
- Harness profiles narrow routing and optional skill exposure; task versions persist.
- Retrieval checks authority again after awaited operations; hard filters stay intact.
- Automatic usage reserves against 10% of 24-hour foreground tokens; unknown usage stops it.
- Automatic apply excludes adapters without exact-revision rollback and reconciliation.
- No MCP endpoint can mint host observations, change grading or grant access.

## Validation record

Targets/build passed; frozen full regression: 540 files, 7,164 passed, 4 skipped (42.32m).
First full attempt stopped at the reviewed server hash in the architecture manifest.
After checking unchanged diagram boundaries, only that source hash was refreshed.
One earlier existing host-storage test timed out at 5 seconds; isolated rerun passed.
Failure records remain in host-private test receipts; they are not erased as successes.
Authenticated disposable-fixture persona integration closes a synthetic next-session loop.
Its effect provenance is `synthetic`, never counted as an operational improvement.
Existing non-admin login/whoami/logout passed before/after deployment; no new binding.
NAS-backed deployment/live reads passed; source/world/economy bytes unchanged.

## Remaining work and operational gates

CLI lacks verified host-message/usage transport and evaluator wiring; this is implementation work.
Native world/skill delivery, general resource pins and non-persona rollback remain open.
Direct/graph routing, optional-review removal and four-candidate archive remain open.
Strategy evolution stays absent until 20 actual independently verified cycles exist.
New private evaluations and actual model cost/quality trials have not been performed.
Budget history stops at 1,024 entries; it needs bounded archival for long-term operation.
Operational changes/effects: **0 / 0**. No savings or full-plan completion claimed.
Last whole-skill metadata: **186/1,610 (11.55%)**; active: **0/1,610 (0.00%)**.
These are prior recorded counts, not a new inventory; this slice changes both by **0pp**.
